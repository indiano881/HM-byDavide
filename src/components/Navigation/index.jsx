import styles from './Navigation.module.css'

const Navigation = () => {
    return (
        <>
          <nav className={styles.navigation}>

          </nav>
        </>
    )
};

export default Navigation;
